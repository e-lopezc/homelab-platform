# Routes https://whoami.homelab to the whoami service via Traefik. The cert-manager annotation
# tells ingress-shim to auto-create a Certificate (issued by homelab-ca-issuer) into whoami-tls,
# which Traefik then serves. Change the host here to rename the endpoint.
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: whoami
  namespace: whoami
  annotations:
    cert-manager.io/cluster-issuer: homelab-ca-issuer
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - whoami.homelab
      secretName: whoami-tls
  rules:
    - host: whoami.homelab
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: whoami
                port:
                  number: 80
