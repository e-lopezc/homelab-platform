# Cluster config that depends on the controllers above (e.g. ClusterIssuer,
# IngressClass). Intentionally empty for Phase 2.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources: []
