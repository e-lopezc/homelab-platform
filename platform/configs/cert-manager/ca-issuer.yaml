# The workhorse issuer: signs leaf certs for services using the root CA above.
# Reference this from a Certificate or an Ingress annotation to get a trusted cert.
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: homelab-ca-issuer
spec:
  ca:
    secretName: homelab-root-ca
